{% extends 'base-admin.html.twig' %}

{% block seo %}<title>{{ 'admin.country.meta-title.text'|trans }}</title>{% endblock %}


{% block body %}
    <h1 class="h1">{{ 'admin.country.h1.text'|trans }}</h1>
    <div class="admin-link-dashbord">
        <ol><li><a href="{{ url('admin_country_add') }}" title="{{ 'admin.country.add.country.text'|trans }}">{{ 'admin.country.add.country.text'|trans }}<i class="fa-solid fa-square-plus"></i></a></li></ol>
    </div>

    <table>
    <thead>
        <tr>
            <th>id</th>
            <th>isocode</th>
            <th>sapa</th>
            <th>action</th>
        </tr>
    </thead>
    <tbody>
        {% for country in countries %}
            <tr>
                <td>{{ country.id }}</td>
                <td>{{ country.isocode }}</td>
                <td>{{ country.sapa }}</td>
                <td>
                    <a href="{{ url('admin_country_edit', { id:country.id } ) }}" title="{{ 'admin.country.action.edit.text'|trans }}">{{ 'admin.country.action.edit.text'|trans }}<i class="fa-solid fa-pen-to-square"></i></a>
                    -
                    <a href="{{ url('admin_country_remove', { id:country.id } ) }}" title="{{ 'admin.country.action.delete.text'|trans }}">{{ 'admin.country.action.delete.text'|trans }}<i class="fa-solid fa-trash"></i></a>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}